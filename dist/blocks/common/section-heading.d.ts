import type { FC, ReactNode } from "react";
interface SectionHeadingProps {
    children: ReactNode;
    className?: string;
}
declare const SectionHeading: FC<SectionHeadingProps>;
export default SectionHeading;
